# セキュリティとプライバシー

ClipNestは127.0.0.1だけで待ち受けます。LAN・インターネットにポート公開しないでください。ブラウザの同一オリジン検証は行いますが、同じ端末で動く悪意あるプログラムから隔離するものではありません。

GitHubでPrivate vulnerability reportingが有効な場合はSecurityタブから非公開で報告してください。未設定の場合、公開Issueに再現用の秘密情報や攻撃手順を投稿せず、まず非公開の連絡方法を問い合わせてください。

- DBには視聴・検索傾向が含まれます。公開物・Issueへ添付しないでください。
- ログはコード・ステータスと起動情報だけを記録し、最大約1MBでローテーションします。検索語・動画ID・署名付きURL・生の例外を意図的に記録しません。
- 設定の診断ダウンロードは許可した項目だけを出力します。既存の「状態 → 再生・シーク診断」には動画IDや再生位置が含まれるので、共有する場合は自分で伏せてください。
- yt-dlp更新は利用者の操作時のみ公式GitHubへ通信し、配布されたSHA-256を照合します。同じ配布元のチェックサムとの一致は、独立した署名の検証ではありません。異なる配布元へのリダイレクトは拒否します。
- YouTube・サムネイル・PyPIの版確認・モデル取得など、機能に必要な通信があります。オフライン専用ではありません。

更新対象は原則として最新リリースです。YouTubeの仕様変更に伴う取得失敗をセキュリティ欠陥として扱う必要はありません。利用規約・コンテンツの権利・各VOICEVOX音声の利用条件を守ってください。
