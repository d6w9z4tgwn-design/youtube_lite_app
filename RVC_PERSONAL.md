# RVC 声変換セットアップ

ClipNest の「RVC 声変換」を利用するための公開ユーザー向けセットアップ手順です。

RVC の処理環境や音声モデルは、ClipNest の通常起動だけでは自動インストールされません。RVC を利用する場合のみ、以下の手順で処理環境を準備してください。

音声モデルは公開配布物に同梱していません。利用者自身が利用条件を確認した RVC モデルを用意し、ClipNest へインポートします。

## 必要環境

RVC の初回セットアップには次が必要です。

- Python 3.11
- Git
- FFmpeg
- インターネット接続
- Python依存ライブラリ、RVCエンジン、解析モデルなどを保存するための数GB程度の空き容量

RVCセットアップスクリプトは **Python 3.11** で実行してください。

GPUは必須ではありません。CPUでも利用できます。対応環境ではCUDAまたはApple GPUを利用できます。

## リポジトリを取得

まだClipNestのソースコードを取得していない場合は、次を実行します。

```sh
git clone https://github.com/d6w9z4tgwn-design/youtube_lite_app.git
cd youtube_lite_app
```

すでに取得済みの場合は、そのリポジトリのフォルダへ移動してください。

## RVC処理環境をセットアップ

### macOS / Linux

```sh
python3.11 scripts/setup_rvc.py
```

### Windows

Python Launcherを使用している場合は次のように実行できます。

```powershell
py -3.11 scripts/setup_rvc.py
```

セットアップスクリプトは、ClipNest本体とは分離したRVC専用Python環境を作成し、`requirements-rvc.txt` の依存ライブラリ、固定リビジョンのRVCエンジン、ContentVec・RMVPEなど必要な解析リソースを準備します。

取得する固定リソースはSHA-256で検証されます。

ClipNestを起動しただけ、または「RVC 声変換」をONにしただけでは、このセットアップ処理やモデルのダウンロードは実行されません。

すでに依存ライブラリをインストール済みで、依存ライブラリの再インストールを省略する場合は次を利用できます。

```sh
python3.11 scripts/setup_rvc.py --skip-install
```

通常の初回セットアップでは `--skip-install` を付けないでください。

## ずんだもんモデルを使用する場合

ずんだもん（zundamon-1）のモデル本体は、このGitHubリポジトリには同梱されていません。

利用する場合は、RVCセットアップ時に `--zundamon-personal` を指定します。

macOS / Linux:

    python3.11 scripts/setup_rvc.py --zundamon-personal

Windows:

    py -3.11 scripts/setup_rvc.py --zundamon-personal

このオプションを指定すると、通常のRVC処理環境のセットアップに加えて、ずんだもん（zundamon-1）のモデルを外部配布元から取得します。

ダウンロード開始前に、モデル配布元と関連する利用条件がターミナル上に表示されます。

利用者が内容を確認し、

    利用条件を確認し、自身の責任で使用することに同意しますか？ [y/N]:

という確認に `y` または `yes` と入力した場合のみ、モデルをダウンロードします。

それ以外の入力では、ずんだもんモデルの取得を中止します。

モデルはGitHubリポジトリ内には保存されず、各ユーザーのClipNestデータディレクトリ内に保存されます。

利用条件への適合性やモデルの使用可否については、利用者自身で確認・判断してください。

## 保存先

RVCの環境、モデル、一時キャッシュはClipNestのユーザーデータディレクトリ内の `rvc/` に保存されます。

| OS | RVC保存先 |
| --- | --- |
| Windows | `%LOCALAPPDATA%/ClipNest/rvc/` |
| macOS | `~/Library/Application Support/ClipNest/rvc/` |
| Linux | `$XDG_DATA_HOME/ClipNest/rvc/`、未設定の場合は `~/.local/share/ClipNest/rvc/` |

`CLIPNEST_DATA_DIR` を設定している場合は、そのディレクトリ内の `rvc/` を使用します。

主な内容は次の通りです。

- `models/`：インポートしたRVCモデルと検証情報
- `runtime/`：RVC専用Python 3.11環境
- `engine/`：RVC処理エンジン
- `separation/`：人声分離などに使用するリソース
- `cache/`：実行中セッションの一時音声

## RVCモデルを追加

処理環境のセットアップ後、ClipNestを起動します。

1. 「拡張機能」で **RVC 声変換** をONにします。
2. RVCの設定を開きます。
3. **別の声を追加（モデルのインポート）** を選びます。
4. 信頼できる配布元から入手した推論用 `.pth` を選択します。
5. 同じモデル用の `.index` がある場合は併せて選択します。
6. モデルの利用条件を確認し、確認欄にチェックして追加します。

表示名はファイル名から自動入力され、追加したモデルが選択されます。

### 対応形式・制限

- 標準RVC v1/v2、HiFi-GAN、32/40/48kHzに対応しています。
- すべてのRVCモデルの動作を保証するものではありません。
- `.pth` は128MBまでです。
- `.index` は256MBまでです。
- 保存できるモデルは20個までです。
- アーカイブや任意スクリプトはインポートできません。
- モデル読み込みには `torch.load(weights_only=True)` を使用します。
- 任意コード実行を必要とするモデル形式へのフォールバックは行いません。
- 各モデルは別IDで保存され、既存モデルやindexを上書きしません。
- indexの追加だけが失敗した場合でも、正常に保存されたモデル本体は利用できます。

安全な読み込み方式だけで悪意のあるファイルを完全に無害化できるわけではありません。信頼できる配布元のモデルだけを使用してください。

ClipNestは音声モデルそのものの利用許諾を提供しません。モデルのライセンス、利用規約、声の権利などは利用者自身で確認してください。

## 使い方

1. 動画を音声モードで再生します。
2. RVCの設定を開きます。
3. 使用する音声モデルを選択します。
4. **声を変えて再生** を押します。

現在位置付近から変換を開始し、最初の短い区間が準備できると再生を開始します。動画全体の変換完了を待つ必要はありません。

「標準」「声を中心に」「声だけ」で声とBGMのバランスを切り替えられます。

「声」「BGM」の個別調整は再生中にも反映されます。全体の音量・ブーストは通常の音量バーで操作します。

変換待ちは「中止して原音を再生」で中止できます。

「原音に戻す」を選択すると原音へ戻ります。再生中の場合は原音で再生を続け、一時停止中の場合は一時停止状態を維持します。

RVC声変換自体をOFFにした場合は、安全のため処理・再生を停止します。

## 詳細調整

「詳細調整」では次を変更できます。

- 音高
- モデルへの寄せ方
- 処理デバイス

通常は次の設定のまま利用できます。

- 処理デバイス：自動（GPU優先）
- 音高：0
- モデルへの寄せ方：0.65

設定を変更した場合は、再度「声を変えて再生」を実行してください。

## CPU / GPU

自動選択では、利用可能なデバイスを次の順で使用します。

1. CUDA
2. Apple GPU
3. CPU

GPUで処理できない場合はCPUへフォールバックし、そのRVCプロセスではCPUを使用し続けます。

CPUおよびApple Silicon GPUでの動作確認があります。CUDA環境は構成によって依存関係や動作が異なる可能性があります。

## 処理方法

音声は端末内で次の順に処理されます。

1. Demucsで元音声から人声を分離
2. ContentVec・RMVPE・RVCで人声を変換
3. 分離したBGMと変換後の声を合成

外部のTTSサーバーやAPIキーは必要ありません。

ただし、初回セットアップで必要なリソースを取得するとき、およびYouTubeから音声を取得するときはインターネット接続が必要です。

## チャンク変換

動画全体を最初に変換する方式ではありません。

現在位置から次の順に準備します。

1. 最初の4秒
2. 続く8秒
3. 以降20秒ずつ

短い最初の区間から再生し、その間に後続区間を先読みします。

未変換の位置へシークした場合も、移動先から短い区間を作成して再開します。

変換済みキャッシュ内へ移動した場合は再変換せずに再生できます。保持範囲外へ移動した場合は古い区間を破棄し、新しい再生位置を優先します。

サーバー側では最大6区間、ブラウザ側では最大4区間の変換結果を保持します。

## モデルの再利用とメモリ解放

一度読み込んだ次のモデルは、同じRVCプロセス内で再利用されます。

- 人声分離モデル
- RVC
- ContentVec
- RMVPE

モデルまたは処理デバイスを変更した場合は必要なモデルを読み直します。

処理しない状態が2分続くとRVCプロセスを終了し、メモリを解放します。

正常終了時にはサーバー側の一時音声を削除します。強制終了した場合は一時ファイルが残る場合があります。

## 処理速度について

RVCは常時リアルタイムで変換できることを保証する機能ではありません。

処理速度はCPU/GPU性能、使用するRVCモデル、人声分離処理、初回モデル読み込み、動画・音声の内容、ネットワーク状況などによって変わります。

変換が再生に追いつかない場合は一時停止し、次の区間の準備後に再開します。

この機能は動画全体を変換済み音声ファイルとして保存する機能ではありません。再生位置付近だけを変換・保持します。

## 再生機能との連携

プレイヤーの元のaudio要素を時刻・シーク・キューの基準として維持します。

変換音声のサンプルレートを適切に変換し、声とBGMを同じ長さに揃えます。

既存の再生速度・ピッチ機能は変換音声にも適用されます。

VOICEVOX吹替とは同時に使用できません。

## 現在の制限

現在、次の機能には対応していません。

- 公式埋め込み映像との同期
- 話者ごとのRVCモデル選択
- 複数話者のうち特定人物だけを変換

人声分離は完全ではありません。

歌、複数話者、BGM、効果音などによって分離漏れやにじみが発生することがあります。チャンク境界で音質が変化する場合もあります。

## 利用条件・出典

RVCモデルや元動画には、それぞれ配布者・権利者の利用条件が適用されます。

使用するモデルのライセンス・利用規約を確認してください。また、変換結果を元の話者本人の発言と誤認させるような利用は避けてください。

ClipNestのRVC処理環境では主に次を使用します。

- [Applio](https://github.com/IAHispano/Applio) — 固定リビジョンのSynthesizer、ContentVec読み込み、RMVPE
- [Demucs](https://github.com/facebookresearch/demucs) — 人声分離（HTDemucs）

固定リビジョン、取得元、SHA-256などの詳細は `rvc_assets.py` を参照してください。

特定の音声モデルや利用者の個人データを、このリポジトリの公開配布物へ自動的に含めることはありません。
