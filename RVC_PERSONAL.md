# 個人用 RVC 声変換

このMacには **ずんだもん（zundamon-1）** と処理環境を準備済みです。字幕・文字起こし・音声ファイルの用意は不要です。公開用の `.app`・配布ZIP・ビルド設定は変更していません。

## 使う

1. 起動中の旧ClipNestを終了し、`Start ClipNest Personal.command` をダブルクリックします。これは今回のソース版を起動します。既存の配布用 `.app` ではありません。
2. アプリの「拡張機能」で **RVC 声変換** をONにします。
3. 動画を音声モードで再生し、RVCの設定を開きます。
4. ずんだもんを選んだまま **声を変えて再生** を押します。現在位置から最初の4秒ができると再生し、その間に続きを変換します。数十分の動画でも全体の完成は待ちません。

「標準」「声を中心に」「声だけ」でバランスを切り替えられます。「声」「BGM」の個別調整は再生中も反映されます。全体の音量・ブーストは従来の音量バー一つで操作します。

変換待ちは「中止して原音を再生」で中止できます。再生中の「原音に戻す」は原音で再生を続け、一時停止中に戻す場合は一時停止を保ちます。拡張機能自体をOFFにした場合は安全のため停止します。

音高・モデルへの寄せ方・処理デバイスは「詳細調整」にあります。通常は自動（GPU優先）・音高0・寄せ方0.65のままで利用できます。以前にCPUを選んでいた場合はその設定を維持します。「高速化を有効にする（GPU自動選択）」で切り替えられます。変更後は再度「声を変えて再生」を押してください。

自動選択ではCUDA、Apple GPU、CPUの順に利用可能なものを選びます。GPUで処理できない場合はCPUへ切り替え、その起動中はCPUを使い続けます。CPUとApple M5のGPUで動作確認済みです。CUDAは未検証です。

## 別のモデルを追加

「別の声を追加（モデルのインポート）」で、信頼できる配布元の推論用 `.pth` を選びます。表示名はファイル名から自動入力されます。同じモデル用の `.index` があれば併せて選択してください。利用条件の確認にチェックを入れて追加すると、その声が自動選択されます。

- 標準RVC v1/v2、HiFi-GAN、32/40/48kHzに対応。モデルによっては利用できません。
- `.pth` は128MBまで、`.index` は256MBまで、モデルは20個までです。アーカイブや任意スクリプトはインポートできません。
- `torch.load(weights_only=True)` を使用し、任意コードの実行が必要なモデルへ切り替えるフォールバックはありません。ただし、安全な読み込みだけで悪意あるファイルを完全に無害化できるわけではありません。信頼できるモデルだけを使用してください。
- モデルは別IDで保存し、既存のモデル・indexを上書きしません。index追加だけが失敗した場合も、保存できたモデル本体は利用できます。

## 待ち時間と制限

Demucsによる人声分離 → ContentVec・RMVPE・RVCによる声変換 → 元の音声から分離したBGMと合成、の順で端末内処理します。外部のTTSサーバーやAPIキーは不要です。YouTubeから音声を取得するためのネット接続は必要です。

最初の4秒 → 続く8秒 → 以降20秒ずつ、の順に準備します。短い最初の区間から再生し、次の区間を先読みします。未変換の区間への移動時も、移動先から4秒を作って再開します。変換済みのキャッシュ内への移動は再変換せず再生できます。保持している範囲の外へ移動した場合は古い区間を破棄して、新しい再生位置を優先します。

一度読み込んだ人声分離・RVC・ContentVec・RMVPEのモデルを同じプロセスで再利用します。GPUも利用し、従来のモデル・サンプルレート・分離設定は維持しています。モデルまたはデバイスを変更した場合は読み直します。処理しない状態が2分続くとプロセスを終了してメモリを解放します。

2026-09-11、Apple M5・24GBでのローカルテスト（合成読み上げ＋BGM、ずんだもん、20秒＋前後各1秒）では、CPU初回14.1秒、GPU初回8.2秒、GPUのモデル再利用時4.3秒でした。ネット取得・ブラウザへの受け渡し時間は含みません。再現用スクリプトは `scripts/benchmark_rvc.py` です。

別途、音声取得から4秒→8秒→20秒を実際に通すテストでも区間長・再利用を確認しました。この測定では初回4秒の準備に14.5秒、後続8秒に3.1秒、後続20秒に6.6秒かかりました。負荷や初回初期化で時間は変動し、「4秒区間だから4秒以内に準備できる」という保証ではありません。

**初回モデル準備・ネット取得の待ち時間は残り、常時リアルタイムで変換できる保証はありません。** 変換が再生に追いつかない場合は一時停止し、区間の準備後に再開します。再生位置付近だけを変換・保持する仕組みで、動画全体を変換済みファイルとして保存する機能ではありません。実動画では速度や品質が変わります。

プレイヤーの元のaudio要素を時刻・シーク・キューの基準として維持します。変換音声のサンプルレートを正しく変換し、声とBGMを同じ長さに揃えます。再生速度とピッチの既存機能は変換音声にも適用します。VOICEVOX吹替とは排他使用です。

公式埋め込み映像との同期、話者ごとの選択、特定の人物だけの変換には未対応です。人声分離は完全ではなく、歌・複数話者・効果音などに分離漏れやにじみが残る場合があります。チャンク境界でも音質が変わる場合があります。

## 保存先・セットアップ

このMacの保存先は `~/Library/Application Support/ClipNest/rvc/` です。

- `models/`：個人用プリセット・追加モデル・検証情報
- `runtime/`：アプリ本体と分離したPython 3.11環境
- `engine/`、`separation/`：処理エンジンと固定バージョンの解析モデル
- `cache/`：実行中セッションの音声。サーバーは最大6区間、ブラウザは最大4区間の変換結果を保持します。正常終了時にサーバーの一時音声を削除します。強制終了時には残る場合があります。

初回セットアップをやり直す場合のみ、ソースフォルダで次を実行します。Python 3.11・Git・FFmpegと、依存ライブラリを含む数GBの空き容量が必要です。

```sh
python3.11 scripts/setup_rvc.py --zundamon-personal
```

プリセット不要で処理環境のみ準備する場合は `--zundamon-personal` を外します。起動・モジュールのONだけではインストールやダウンロードを行いません。セットアップは固定リビジョンを取得し、モデルのSHA-256を検証します。

## 利用条件・出典

ずんだもんはユーザー指定の個人用途でのみ準備しています。公開・配布の許諾を代行取得したものではありません。モデル・元動画の利用条件を確認し、変換結果を本人の発言と誤認させる使い方は避けてください。

- [kuwacom/RVC-Models：モデルカード](https://huggingface.co/kuwacom/RVC-Models) — `zundamon-1`、revision `723073a4634626a3595aaa98816c7f14262eddda`
- [ずんだもんのマルチモーダルデータベース・利用条件](https://zunko.jp/multimodal_dev/login.php)
- [Applio](https://github.com/IAHispano/Applio) — revision `7fa68ec2166ab1331c539704159fa14901e94e5a` のSynthesizer、ContentVec読み込み、RMVPEを使用
- [Demucs](https://github.com/facebookresearch/demucs) — 人声分離（HTDemucs）

全リソースの取得元・ハッシュは `rvc_assets.py` に記録しています。モデルや個人データを公開リポジトリへコピーしないでください。
